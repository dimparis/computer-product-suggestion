@model IEnumerable<CPS_Solution.EntityFramework.Product>

@{
    ViewBag.Title = "Compare";
    Layout = "~/Views/Shared/_Compare.cshtml";
}

<h2>Compare</h2>
<div>
    <table class="table stripped" style="width:10%; float:left;">
        <tr>
            <td>asdsd</td>
        </tr> <tr>
            <td>asdsd</td>
        </tr> <tr>
            <td>asdsd</td>
        </tr> <tr>
            <td>asdsd</td>
        </tr>
    </table>
   
    <div class="products-layout grid m-t-b add-cart" style="width:83%; float:right;">
        @foreach (var item in Model) { 
            <div class="product" >
                <div class="entry-main">
                    <h5 class="entry-title" style="border:dotted;"><a href="javascript:void(0)">@Html.DisplayFor(modelItem => item.Name)</a></h5>
                    <input id="isWinner" type="text" value="test" onload="change()" disabled>
                </div>  
                <div class="entry-media">
                    <img src="@Url.Content("~/Template/GFashion/img/1.jpg")" alt="" class="lazyLoad thumb" />
                </div>
                <div class="entry-main">
                    <div class="entry-description">@Html.DisplayFor(modelItem => item.strCPU)</div>
                    <div class="entry-description">@Html.DisplayFor(modelItem => item.strVGA)</div>
                    <div class="entry-description">@Html.DisplayFor(modelItem => item.strRAM)</div>
                    <div class="entry-description">@Html.DisplayFor(modelItem => item.strHDD)</div>
                    <div class="entry-description">@Html.DisplayFor(modelItem => item.strDisplay)</div>
                    <div class="entry-description" id="vietht" entry-score="@item.TotalWeightPoint">@Html.DisplayFor(modelItem => item.TotalWeightPoint)</div>
                    <div class="entry-description"><input name="demo" id="scoreWin" type="hidden" value="@item.TotalWeightPoint" /></div>
                </div>
            </div>
        }
    </div>
</div>

<script type="text/javascript">
    $(document).ready(function () {
        var acc = $('input[type="hidden"]').map(function () {
            return $(this).val();
        }).toArray();
        console.log(acc);
        var temp = 0;
        for (i = 0; i < acc.length; i++) {
            if (acc[i] >= a[i+1]) {
                acc[i] = temp;
            }

        }
    });
</script>




